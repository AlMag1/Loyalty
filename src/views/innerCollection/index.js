import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const InnerCollection = () => {
  return (
    <View>
      <Text>inner collection screen</Text>
    </View>
  );
};

export default InnerCollection;

const styles = StyleSheet.create({});
